# HTTP server
PORT=3001

# Postgres settings
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=mini_derms

# MQTT broker settings
MQTT_HOST=localhost
MQTT_PORT=1883

# Control loop behavior
CONTROL_INTERVAL_SECONDS=60      # run controller every 60 seconds
FEEDER_DEFAULT_LIMIT_KW=250     # feeder power limit in kW when no DR event
